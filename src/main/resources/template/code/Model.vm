package io.github.touchsun.tstudy.modules.${module}.${function}.model;

import io.github.touchsun.tstudy.common.model.BaseModel;
import lombok.Data;
import lombok.EqualsAndHashCode;
import org.springframework.data.mongodb.core.mapping.Document;

/**
* The ${remark} doc.
*
* @author touchsun
* @since 2024/7/25 21:40
*/
@Document("${module}_${function}")
@Data
@EqualsAndHashCode(callSuper = true)
public class ${beanName} extends BaseModel {

#foreach($field in $fields)
    /**
    * ${field.fieldRemark}
    */
    private String ${field.fieldName};
#end

}
