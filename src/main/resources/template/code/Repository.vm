package io.github.touchsun.tstudy.modules.${module}.${function}.repository;

import io.github.touchsun.tstudy.modules.${module}.${function}.model.${beanName};
import org.springframework.data.mongodb.repository.ReactiveMongoRepository;

/**
* ${beanName}Repository Class
* db operator, reactive repo fill mono or flux return type, and reactive db operator
*
* @author touchsun
* @since 2024/7/25 22:23
*/
public interface ${beanName}Repository extends ReactiveMongoRepository<${beanName}, String> {

}